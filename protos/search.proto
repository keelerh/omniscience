syntax = "proto3";

package omniscience;

import "document.proto";

service Search {
    // Searches all documents based on a simple text query.
    rpc Query (QueryDocumentsRequest) returns (stream Document) {}
}

message QueryDocumentsRequest {
    string query = 1;
}
